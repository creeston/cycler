server {
  listen 80;
  listen [::]:80;
  server_name _;

  root /usr/share/nginx/html;

  location = / {
    return 302 /cycler/;
  }

  location /cycler/ {
    try_files $uri $uri/ /cycler/index.html;
  }
}
